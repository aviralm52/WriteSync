const Document = ({ id }: { id: string }) => {
  return (
    <div>
      <form action="">
        {/* update title... */}
      </form>

      <div>
        {/* Manage Users */}

        {/* Avatars */}
      </div>

      {/* Collaborative Editor */}
    </div>
  );
};
export default Document;
